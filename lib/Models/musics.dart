class Music {
  late String artistName,title,albumTitle,preview,imageMusic;//late ou ? pour ne pas initialiser maintenant
    
  Music(this.artistName,this.title, this.albumTitle,this.preview,this.imageMusic);

}